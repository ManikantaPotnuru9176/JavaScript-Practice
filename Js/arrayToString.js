const string_test = ["mani", "abdhul", "praveen", "ramu", "revathi"];

// function to join all elements of the following array into a string
// using join method
const array_to_string = (input) => input.join();

//using toString method
const to_string = (input) => input.toString();

console.log(array_to_string(string_test));

console.log(to_string(string_test));
